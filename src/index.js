import createBrowserHistory from 'history/createBrowserHistory';
import createHashHistory from 'history/createHashHistory';
import router from './router';

export { createBrowserHistory, createHashHistory, router };
